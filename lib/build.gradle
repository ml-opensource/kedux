plugins {
    id 'org.jetbrains.kotlin.multiplatform' version '1.3.70'
}
group 'com.example'
version '0.0.1'

apply plugin: 'maven-publish'

ext {
    reaktiveVersion = '1.1.11'
}

kotlin {
    jvm()
    js {
        browser {
        }
        nodejs {
        }
    }
    // For ARM, should be changed to iosArm32 or iosArm64
    // For Linux, should be changed to e.g. linuxX64
    // For MacOS, should be changed to e.g. macosX64
    // For Windows, should be changed to e.g. mingwX64
    macosX64("macos")
    ios("ios")
    sourceSets {
        commonMain {
            dependencies {
                api kotlin('stdlib-common')
                api "com.badoo.reaktive:reaktive:${reaktiveVersion}"
            }
        }
        commonTest {
            dependencies {
                implementation kotlin('test-common')
                implementation kotlin('test-annotations-common')
                implementation "com.badoo.reaktive:reaktive-testing:${reaktiveVersion}"
            }
        }
        jvmMain {
            dependencies {
                api kotlin('stdlib-jdk8')
                api "com.badoo.reaktive:reaktive-jvm:${reaktiveVersion}"
            }
        }
        jvmTest {
            dependencies {
                implementation kotlin('test')
                implementation kotlin('test-junit')
                implementation "com.badoo.reaktive:reaktive-testing-jvm:${reaktiveVersion}"
            }
        }
        jsMain {
            dependencies {
                api kotlin('stdlib-js')
                api "com.badoo.reaktive:reaktive-js:${reaktiveVersion}"
            }
        }
        jsTest {
            dependencies {
                implementation kotlin('test-js')
                implementation "com.badoo.reaktive:reaktive-testing-js:${reaktiveVersion}"
            }
        }
        macos {
            dependencies {
                api "com.badoo.reaktive:reaktive-macosx64:${reaktiveVersion}"
            }
        }
        macosTest {
            dependencies {
                implementation "com.badoo.reaktive:reaktive-testing-macosx64:${reaktiveVersion}"
            }
        }
        iosArm64 {
            dependencies {
                api "com.badoo.reaktive:reaktive-ios64:${reaktiveVersion}"
            }
        }
        iosArm64Test {
            dependencies {
                implementation "com.badoo.reaktive:reaktive-testing-ios64:${reaktiveVersion}"
            }
        }
        iosArm32 {
            dependencies {
                api "com.badoo.reaktive:reaktive-ios32:${reaktiveVersion}"
            }
        }
        iosArm32Test {
            dependencies {
                implementation "com.badoo.reaktive:reaktive-testing-ios32:${reaktiveVersion}"
            }
        }
        iosX64 {
            dependencies {
                api "com.badoo.reaktive:reaktive-iossim:${reaktiveVersion}"
            }
        }
        iosX64Test {
            dependencies {
                implementation "com.badoo.reaktive:reaktive-testing-iossim:${reaktiveVersion}"
            }
        }
    }
}
